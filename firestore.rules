rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /resume/{doc} {
      // Anyone can read the resume
      allow read: if true;
      // Only admin can write
      allow write: if request.auth != null && request.auth.token.email == 'tuphuongvinh154@gmail.com';
    }
  }
}
