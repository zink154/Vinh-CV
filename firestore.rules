rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /resume/{doc} {
      // Anyone can read the resume
      allow read: if true;
      // Only authenticated users can write (you can restrict to your email later)
      allow write: if request.auth != null;
    }
  }
}
